# Basic Node.js Project

A minimal Node.js project skeleton with a start script.

## How to run

- Install dependencies (if any) and start the app:

```
npm start
```

The server will listen on port 3000 by default.
